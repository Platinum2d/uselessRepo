Lo script deve prendere N+1 parametri, di cui:
   i primi N sono indirizzi assoluti di direttori (N >= 1)
   l'ultimo è una stringa che deve iniziare per . (PAT)

lo script deve cercare nei percorsi indicati tutti i file che finiscono per PAT. A fine ricorsione, creare un mini menù che renda fruibili i file trovati.
